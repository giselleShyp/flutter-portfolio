import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const primary = Color(0xFF3B82F6);

  // Light
  static const lightBackground = Color(0xFFFFFFFF);
  static const lightSurface = Color(0xFFF8FAFC);
  static const lightText = Color(0xFF020817);
  static const lightBorder = Color(0xFFE2E8F0);

  // Dark
  static const darkBackground = Color(0xFF020817);
  static const darkSurface = Color(0xFF0F172A);
  static const darkText = Color(0xFFF8FAFC);
  static const darkBorder = Color(0xFF1E293B);

  // States
  static const success = Color(0xFF22C55E);
  static const error = Color(0xFFEF4444);
  static const warning = Color(0xFFF59E0B);
  // The "Soft White" from your screenshot (Slate 50)
  static const Color offWhite = Color(0xFFF8FAFC);

  // A slightly darker gray for secondary text (Slate 400)
  static const Color mutedText = Color(0xFF94A3B8);
}
